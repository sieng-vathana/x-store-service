server:
  port: ${STORE_SERVICE_PORT:8087}

spring:
  banner:
    location: classpath:banner.txt
    mode: console
  application:
    name: x-store-service
  profiles:
    default: local

x:
  http:
    logging:
      enabled: true
      server: x-store-service
      hop: "x-api-gateway → x-bff → x-store-service"
      hops:
        /api/v1/stores: "x-api-gateway → x-bff → x-store-service"
